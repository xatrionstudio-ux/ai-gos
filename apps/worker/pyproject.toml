[project]
name = "worker"
version = "1.0.0"
description = "AI-GOS Celery Worker and Task Processing Engine"
requires-python = ">=3.12"
dependencies = [
    "celery[redis]>=5.4.0",
    "redbeat>=2.2.0",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30.0",
    "core",
    "events",
    "identity",
    "projects",
    "ai",
    "workflow",
]
