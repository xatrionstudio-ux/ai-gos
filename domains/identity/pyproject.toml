[project]
name = "identity"
version = "1.0.0"
description = "AI-GOS Identity domain — auth, users, organizations, RBAC"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9.0",
    "argon2-cffi>=23.1.0",
    "PyJWT>=2.9.0",
    "cryptography>=43.0.0",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30.0",
    "email-validator>=2.2.0",
    "core",
    "events",
]
