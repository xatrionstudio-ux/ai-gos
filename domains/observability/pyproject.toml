[project]
name = "observability"
version = "1.0.0"
description = "AI-GOS Telemetry, LLM-as-a-Judge Quality Engine & Cost Control"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9.0",
    "sqlalchemy[asyncio]>=2.0.36",
    "core",
    "events",
    "ai",
]
