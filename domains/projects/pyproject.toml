[project]
name = "projects"
version = "1.0.0"
description = "AI-GOS Projects domain — SaaS product profiles, brand voice, CMS settings"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9.0",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30.0",
    "core",
    "events",
    "identity",
]
