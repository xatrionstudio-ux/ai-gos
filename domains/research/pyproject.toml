[project]
name = "research"
version = "1.0.0"
description = "AI-GOS Research domain — SERP, multi-source evidence collection, competitor intelligence"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9.0",
    "sqlalchemy[asyncio]>=2.0.36",
    "core",
    "events",
    "ai",
    "knowledge",
]
