[project]
name = "tenant"
version = "1.0.0"
description = "AI-GOS Multi-Tenant Control Plane, Feature Flags, and Billing Metering Engine"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9.0",
    "sqlalchemy[asyncio]>=2.0.36",
    "core",
    "events",
]
