[project]
name = "events"
version = "1.0.0"
description = "AI-GOS event bus and domain event schemas"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9.0",
    "redis>=5.1.0",
    "celery>=5.4.0",
    "core",
]
